# The governing specification. Never reformatted — it is the source of truth and
# should only ever change when the product owner changes it.
KINVO_BACKEND_BUILD.md

dist
coverage
node_modules
src/generated
package-lock.json
prisma/migrations
# Both API contracts are GENERATED by `npm run docs:export`. Prettier rewrites
# the JSON one into its own style, so a format run after an export flips the
# whole file back and forth and buries a real contract change in noise.
docs/openapi.yaml
docs/realtime.json
